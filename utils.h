#pragma once

#include <sstream>
#include <string>

extern std::string ChToStr(char);

extern float StrToFloat(std::string);

extern bool isalpha_(char);

extern bool isdigit_(char);

extern bool isasciich_(char);