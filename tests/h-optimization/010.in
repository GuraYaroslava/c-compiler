int main()
{
    int a = 24 / ((3 * ((5 + (36 + 4)) / 5)) % 5) >> 2;
    while (a)
    {
        a -= (24 / ((3 * ((5 + (36 + 4)) / 5)) % 5) >> 2) - 2;
        printf("bodyFor\n");
    }
    return 0;
}
