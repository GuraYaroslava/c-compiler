int Add(int a, int b)
{
    return a + b;
}

int main()
{
    if (1)
        return 0;

    int a[3];
    a[12 >> 2 - 3] = 125 + (3 * ((5 + (36 + 4)) / 5)) % 5;
    a[(14 / 2) % 3] = 126 + (3 * ((5 + (36 + 4)) / 5)) % 5;

    printf("%d\n", a[0]);
    printf("%d\n", a[1]);
    printf("%d\n", Add(a[0], a[1]));
    return 0;
}
