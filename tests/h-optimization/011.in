int main()
{
    int a = 0;
    while ((24 / ((3 * ((5 + (36 + 4)) / 5)) % 5) >> 2) - 2)
    {
        if (a > 4) break;
        a += (24 / ((3 * ((5 + (36 + 4)) / 5)) % 5) >> 2) - 2;
        printf("bodyFor\n");
    }
    return 0;
}
