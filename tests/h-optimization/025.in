int Add(int a, int b)
{
    return a + b;
}

int main()
{
    int a = 1, b = 1;

    int Sub(int a, int b)
    {
        int Mul(int a, int b)
        {
            return a * b;
        }
        return Mul(a, a) - Mul(b, b);
    }

    if (0)
    {
        Sub(1, 3);
    }

    printf("%d", Add(a, 5));
    return 0;
}
