struct point{
    float x, y, z;
    int a, b, c;
};

struct point b;

int main()
{
    b.x = b.y = b.z = (b.a = b.b = b.c = -1) * 3.0;
    printf("%f %f %f %d %d %d", b.x, b.y, b.z, b.a, b.b, b.c);
    return 0;
}