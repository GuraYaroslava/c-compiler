int main()
{
    int a = 0, b = 10;
    while (a - b)
    {
        a += 1;
        if (a & 1)
            printf("%d is odd\n", a);
        else 
            printf("%d is even\n", a);
    }
    return 0;
}